<tr>
  <label>
    <%= check_box_tag 'checked', question_response.id, question_response.checked,
        data: {
          remote: true,
          url: question_response_toggle_path(question_response.question, question_response),
          method: :post
        }
    %>
    <%= question_response.text %>
  </label>
</tr>
